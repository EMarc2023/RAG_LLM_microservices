[pytest]
markers =
    unit: Fast tests that mock all network calls.
    integration: Slower tests that require live services.
    smoke: Quick health checks for deployment.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function